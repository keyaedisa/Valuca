# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Your Name <keyaedisa>
pkgname="valuca-zsh-theme"
pkgver=1.0
pkgrel=0
epoch=
pkgdesc="Valuca zsh theme for valen and xerolinux"
arch=('x86_64' 'xi686')
url="https://github.com/keyaedisa/valenOhMyZsh"
#license=('')
#groups=()
depends=("git"
		"zsh"
		"zsh-completions")
makedepends=('git')
#checkdepends=()
#optdepends=()
provides=('valuca-zsh-theme')
#conflicts=('')
#replaces=('')
#backup=()
#options=()
install=valuca.install
#changelog=
source=("git+https://github.com/keyaedisa/Valuca.git")
#noextract=()
#md5sums=()
sha256sums=('SKIP')

package() {
	
}


